# Extracted Knowledge from Conv: 87c0c091-1f2a-445b-b987-e1e651263133

**Date**: 2026-01-17T23:36:57.991523Z

### Extracted Code (bash)

```bash
# 브라우저에서 확인
open https://github.com/corca-ai/corca-plugins

# 또는 curl로 확인
curl -I https://github.com/corca-ai/corca-plugins
```

### Extracted Code (bash)

```bash
# SSH 키 생성
ssh-keygen -t ed25519 -C "your_email@example.com"

# SSH 에이전트 시작
eval "$(ssh-agent -s)"

# SSH 키 추가
ssh-add ~/.ssh/id_ed25519

# 공개 키 복사 (GitHub에 등록)
pbcopy < ~/.ssh/id_ed25519.pub
```

### Extracted Code (bash)

```bash
# 1단계: HTTPS로 시도 (가장 간단)
claude plugin marketplace add https://github.com/corca-ai/corca-plugins.git

# 2단계: 마켓플레이스 목록 확인
claude plugin marketplace list

# 3단계: 플러그인 검색
claude plugin list

# 4단계: g-export 설치
claude plugin install g-export
```
