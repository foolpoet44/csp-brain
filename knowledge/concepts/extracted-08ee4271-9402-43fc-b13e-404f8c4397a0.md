# Extracted Knowledge from Conv: 08ee4271-9402-43fc-b13e-404f8c4397a0

**Date**: 2026-01-24T02:45:21.803592Z

### Extracted Code (text)

```text
예시: "영국 헬스케어 산업을 분석해서 우리 회사 계획을 지원할 
      종합 보고서를 만들어줘. 상위 5-10개 기회를 식별하고
      Slack #product-test 채널에 완료 메시지 보내줘."
```

### Extracted Code (text)

```text
"25년 리더십, 생각하다" 프로그램의 평가 데이터(Excel)를 업로드하고,
개인별 맞춤 피드백 보고서를 100개 자동 생성해줘.
각 보고서는 강점, 개선영역, 액션 플랜을 포함하고
PDF로 저장한 후 파일명은 "이름_리더십평가_2025.pdf"로 해줘.
```

### Extracted Code (text)

```text
글로벌 HR 자동화 SaaS 시장을 분석해줘:
1. 상위 10개 플레이어 식별 (Workday, SAP SuccessFactors 등)
2. 각각의 핵심 기능, 가격 모델, 타겟 고객
3. 시장 갭 및 미충족 니즈 5가지
4. 한국 시장 특수성 고려한 기회 영역
5. HTML 보고서로 정리하고 Notion에 자동 저장
```

### Extracted Code (text)

```text
우리 DX Starter 프로그램의 학습자 진도 데이터(CSV)를 분석하고:
- 모듈별 완료율 시각화
- 낙오 위험군 식별 (진도율 30% 이하)
- 개인별 독려 이메일 초안 작성
- Slack으로 담당자에게 알림 전송
- 주간 대시보드 HTML 생성
```

### Extracted Code (text)

```text
내 Notion 워크스페이스를 스캔해서:
- 태그 없는 페이지 찾아서 자동 태그 추천
- 중복 콘텐츠 식별 및 통합 제안
- 관련 페이지 자동 링크 생성
- "💬 Claude 대화 아카이브" DB와 연결 가능한 페이지 매핑
- 온톨로지 구조 시각화 (Mermaid 다이어그램)
```

### Extracted Code (text)

```text
ESCO API에서 디지털 스킬 온톨로지 데이터를 가져와서:
1. 제조업 맥락에 맞게 필터링
2. 역량-과제-학습경로 관계 매핑
3. SQL 스키마 설계 및 ERD 생성
4. 샘플 데이터 100개 생성
5. Python 데이터 모델 코드 작성
```

### Extracted Code (text)

```text
"25년 리더십, 생각하다" 프로그램 소개 PPT를 만들어줘:
- 슬라이드 1: 타이틀 (LG 브랜드 컬러 적용)
- 슬라이드 2-5: 프로그램 목적, 구조, 대상, 기대효과
- 슬라이드 6-10: 각 모듈 상세 (아이콘 포함)
- 슬라이드 11: 일정 타임라인 (Gantt 차트)
- 슬라이드 12: 문의처
템플릿은 corporate-modern 스타일로.
```

### Extracted Code (text)

```text
리더십 평가 참여자 100명에게 개인화된 초대 이메일 발송:
- 엑셀에서 이름, 이메일, 부서 정보 읽기
- 각 사람에게 맞춤 인사말 (부서별 다른 메시지)
- 평가 링크 개인화 (UTM 파라미터 포함)
- 발송 시간 분산 (스팸 방지)
- 발송 로그 CSV로 저장
```

### Extracted Code (text)

```text
내 포트폴리오 CSV를 분석해서:
- 자산 배분 현황 원그래프
- 월별 수익률 추이 선그래프
- 섹터별 퍼포먼스 히트맵
- 리밸런싱 추천 (목표 비율 대비)
- HTML 대시보드 생성 (매주 자동 업데이트)
```

### Extracted Code (text)

```text
내 eigent-ai/eigent 포크 저장소에서:
- 최근 커밋 변경사항 요약
- Pull Request에 자동 코멘트 (코드 품질 체크)
- Issue 트리아징 (버그/기능요청 분류)
- Release Note 자동 생성
- README 업데이트 제안
```

### Extracted Code (text)

```text
내 Claude 대화 아카이브에서 "HR 자동화" 주제 대화를 찾아서:
1. 핵심 인사이트 10개 추출
2. LinkedIn 포스트 3개 초안 작성 (각 300자)
3. 블로그 글 1개 (1500자, SEO 최적화)
4. 트위터 스레드 (10개 트윗)
5. Notion에 콘텐츠 캘린더 생성
```

### Extracted Code (text)

```text
내 Notion 워크스페이스의 "💬 Claude 대화 아카이브" 데이터베이스를 분석해줘:

1. 최근 30일간 저장된 대화 중 "개발/코딩" 주제 찾기
2. 각 대화의 핵심 키워드 3개씩 추출
3. 태그가 없는 페이지에 자동으로 태그 추가
4. 결과를 표로 정리해서 보여주고
5. 변경사항을 Notion에 반영할지 물어봐

참고: 데이터베이스 URL은 https://www.notion.so/5f2ff32ac1b84981988bf5e0da6eeb15
```

### Extracted Code (text)

```text
┌─────────────────────────┬──────────────────────┬─────────────────┐
│ 페이지 제목              │ 추출된 키워드         │ 추천 태그        │
├─────────────────────────┼──────────────────────┼─────────────────┤
│ Python 에러 해결        │ API, 디버깅, FastAPI │ Python, 개발    │
│ Notion 자동화 구축      │ MCP, 연동, 스크립트  │ Notion, 자동화  │
└─────────────────────────┴──────────────────────┴─────────────────┘

총 15개 페이지를 분석했습니다. 
8개 페이지에 태그를 추가하시겠습니까? (yes/no)
```

### Extracted Code (text)

```text
다음 교육 참여 데이터를 분석해줘:

[CSV 데이터 붙여넣기 또는 파일 업로드]

과정명,참여자수,완료율,만족도
DX기초,45,78%,4.2
리더십역량,32,65%,4.5
디지털전환,28,82%,4.8
데이터분석,51,71%,4.3

다음을 수행해줘:
1. 완료율 평균 계산
2. 과정별 막대 그래프 생성 (완료율, 만족도)
3. 개선이 필요한 과정 2개 추천 (근거 포함)
4. HTML 보고서 생성
5. 보고서 파일 다운로드 링크 제공
```

### Extracted Code (text)

```text
첨부한 리더십_평가_데이터.xlsx 파일을 기반으로 
개인별 피드백 레터를 생성해줘.

템플릿 구조:
---
[이름]님께,

2025년 리더십 평가 결과를 안내드립니다.

**종합 점수**: [점수]/100
**강점 역량**: [상위 3개 역량]
**개선 역량**: [하위 2개 역량]

**상세 피드백**:
[각 역량별 해석 및 조언 3-4문장]

**액션 플랜**:
1. [구체적 개선 행동 1]
2. [구체적 개선 행동 2]
3. [구체적 개선 행동 3]

**추천 학습 자료**:
- [역량별 추천 과정/도서]

2025년 성장을 응원합니다.
LG PRI 인재개발팀
---

조건:
- 총 50명분 생성
- 각 레터는 개인 맞춤형이어야 함
- Markdown 형식으로 저장
- 파일명: "피드백레터_{이름}.md"
- 생성 완료 후 ZIP 파일로 압축

첫 3개만 먼저 보여주고 확인받은 후 나머지 진행해줘.
```

### Extracted Code (text)

```text
한국 시장의 HR SaaS 경쟁 환경을 분석하고 전략 보고서를 작성해줘:

**1단계: 시장 플레이어 식별**
- 국내 HR SaaS 상위 10개 기업 찾기
- 각 기업의 핵심 제품, 타겟 고객, 가격 정책
- 최근 1년 펀딩 뉴스나 M&A 동향

**2단계: 기능 매트릭스 작성**
다음 기능별로 제공 여부 조사:
- 리더십 평가/다면평가
- 역량 관리 (스킬 온톨로지)
- 학습 관리 (LMS)
- 성과 관리 (OKR/KPI)
- 조직 진단
- AI 기능 (자동화, 분석)

**3단계: 갭 분석**
- 한국 기업들이 겪는 미충족 니즈 5가지
- 글로벌 솔루션 대비 로컬 솔루션의 약점
- 특히 "제조업" 섹터의 특수 요구사항

**4단계: 포지셔닝 제안**
- 내가 진입할 수 있는 틈새 시장 3가지
- 각 시장의 예상 규모, 진입 난이도, 차별화 포인트

**산출물**:
- 경쟁사 비교표 (HTML 테이블)
- 포지셔닝 맵 (이미지)
- 전략 요약 (2페이지 PDF)
- 전체 상세 보고서 (Notion 페이지로 저장)

Notion 저장 위치: "프로젝트" 폴더 하위에 "HR SaaS 시장분석_2025" 페이지 생성
```

### Extracted Code (text)

```text
DX Starter 교육 프로그램의 실시간 모니터링 대시보드를 만들어줘.

**데이터 소스**: 첨부한 dx_learner_progress.csv
(컬럼: 학습자ID, 이름, 부서, 모듈1~5 완료율, 마지막 접속일, 총 학습시간)

**대시보드 구성**:

1. **전체 현황 카드**
   - 총 등록자 수
   - 평균 진도율
   - 완료자 수 (100% 달성)
   - 낙오 위험군 (2주간 미접속)

2. **모듈별 완료율 차트**
   - 가로 막대 그래프
   - 각 모듈당 완료자 수 표시

3. **부서별 진도 비교**
   - 히트맵 형식
   - 부서 x 모듈별 평균 완료율

4. **위험군 리스트**
   - 표 형식
   - 이름, 부서, 진도율, 마지막 접속일
   - 진도율 30% 미만자만 표시

5. **자동 액션**
   - 위험군에게 독려 메일 초안 작성 (개인별)
   - Slack #dx-education 채널에 주간 요약 포스팅

**기술 요구사항**:
- HTML + Chart.js 사용
- 반응형 디자인 (모바일 뷰 지원)
- 매주 월요일 오전 9시 자동 업데이트 (cron 설정)
- 보고서 URL을 Notion "DX 교육 관리" 페이지에 임베드

첫 버전 대시보드를 먼저 보여주고 피드백 받은 후 자동화 설정할게.
```

### Extracted Code (text)

```text
제조업 특화 HR 스킬 온톨로지 시스템을 구축해줘.

**Phase 1: 데이터 수집**
1. ESCO API에서 디지털 스킬 카테고리 전체 가져오기
   - API 엔드포인트: https://ec.europa.eu/esco/api/
   - 필터: occupation type = "occupation", 섹터 = "manufacturing"

2. 한국산업인력공단 NCS 데이터 크롤링
   - URL: https://www.ncs.go.kr
   - 타겟: "생산관리", "품질관리", "스마트제조" 직무

**Phase 2: 데이터 정제 및 매핑**
1. ESCO 스킬 → NCS 능력단위 매핑 (유사도 기반)
2. 중복 제거 및 통합
3. 계층 구조 재구성:
   - Level 1: 직무군 (예: 생산관리)
   - Level 2: 직무 (예: 공정관리)
   - Level 3: 능력단위 (예: 공정분석)
   - Level 4: 스킬 (예: 데이터 분석, Python)

**Phase 3: 관계 정의**
- 스킬 → 학습자원 매핑
- 스킬 → 과제 매핑
- 선수 스킬 관계 (prerequisite)
- 스킬 클러스터 생성

**Phase 4: 데이터베이스 구축**
1. PostgreSQL 스키마 설계 (ERD 생성)
2. 샘플 데이터 1000개 생성
3. SQLAlchemy ORM 모델 코드 작성 (Python)
4. 초기 데이터 마이그레이션 스크립트

**Phase 5: 시각화**
- 온톨로지 그래프 (Mermaid 다이어그램)
- 대시보드 (Streamlit 앱)

**산출물**:
1. 정제된 스킬 데이터 (JSON, CSV)
2. ERD 이미지
3. Python 코드 (모델, 마이그레이션)
4. 온톨로지 문서 (Notion 페이지)
5. 시각화 대시보드 (Streamlit 배포 URL)

각 Phase 완료 후 결과를 보여주고 다음 단계 진행 여부를 물어봐줘.
```

### Extracted Code (text)

```text
내 Notion "💬 Claude 대화 아카이브"에서 지난 3개월간 
"HR 자동화" 관련 대화를 분석해서 콘텐츠 시리즈를 만들어줘.

**Step 1: 인사이트 추출**
- 대화에서 언급된 핵심 개념 20개 추출
- 각 개념의 빈도수 및 맥락 분석
- Top 10 인사이트 선정

**Step 2: 콘텐츠 생성**

A. LinkedIn 포스트 시리즈 (5개)
   - 각 300-400자
   - 스토리텔링 톤
   - 해시태그 5개씩
   - 이미지 제안 (DALL-E 프롬프트 포함)
   - 게시 요일: 월/수/금 오전 9시

B. 블로그 글 (1개, 심화)
   - 1500-2000자
   - 제목: SEO 최적화 (키워드: HR 자동화, AI, 온톨로지)
   - 구조: 문제정의 → 솔루션 → 사례 → 행동촉구
   - Meta description, OG 태그 포함

C. Twitter/X 스레드 (1개)
   - 10개 트윗
   - 각 트윗 280자 이내
   - 첫 트윗: Hook (질문형)
   - 마지막 트윗: CTA (블로그 링크)

**Step 3: 콘텐츠 캘린더**
- Notion "콘텐츠 관리" DB에 자동 입력
- 속성: 게시일, 채널, 상태(초안), 주제, 키워드
- 2주간의 발행 일정

**Step 4: 자동화 설정**
- Slack #marketing 채널에 "콘텐츠 준비 완료" 알림
- 각 게시일 D-1에 리마인더 발송

모든 콘텐츠를 먼저 보여주고, 
CSP님이 선택한 것만 Notion에 저장할게요.
```

### Extracted Code (text)

```text
"25년 리더십, 생각하다" 프로그램의 전체 운영을 자동화해줘.

**입력 데이터**:
- 참여자 명단 (Google Sheets)
- 평가 설문 결과 (Typeform → CSV)
- 학습 자료 (Notion 페이지 링크들)

**자동화 워크플로우**:

1️⃣ **사전 준비 단계**
   - 참여자에게 웰컴 이메일 발송 (캘린더 초대 첨부)
   - Slack 비공개 채널 자동 생성 및 초대
   - Notion 개인 워크스페이스 페이지 생성

2️⃣ **진행 중 모니터링**
   - 매주 월요일: 학습 진도 체크 (Notion 페이지 조회수 기반)
   - 수요일: 중간 설문 발송 (Typeform)
   - 금요일: 주간 요약 리포트 (Slack 포스팅)

3️⃣ **평가 단계**
   - Typeform 결과 자동 수집
   - 개인별 피드백 레터 생성 (앞서 예시 3 활용)
   - 강점/개선영역 시각화 차트 (레이더 차트)

4️⃣ **사후 관리**
   - 3개월 후 Follow-up 설문 자동 발송
   - 변화 추적 리포트 (Before/After 비교)
   - 우수 사례 자동 식별 및 문서화

5️⃣ **종합 보고**
   - 경영진 보고용 대시보드 (HTML)
   - 프로그램 효과성 분석 (ROI 계산)
   - 다음 기수 개선안 도출

**기술 스택**:
- Google Sheets API (참여자 데이터)
- Notion API (페이지 생성/조회)
- Slack API (메시지 발송)
- Gmail API (이메일 발송)
- Typeform Webhook (실시간 응답 수신)

**Human-in-the-Loop 포인트**:
- 이메일 템플릿 최종 승인 (1회만)
- 주간 리포트 검토 후 발송 승인
- 경영진 보고서 최종 검토

각 단계별로 테스트 실행 → 검증 → 본 실행 순으로 진행할게요.
첫 주는 파일럿으로 소그룹(5명)만 대상으로 해볼까요?
```

### Extracted Code (text)

```text
[목적] 명확히 정의

[입력 데이터]
- 데이터 소스 명시
- 파일 형식 및 컬럼 설명

[처리 단계]
1. [구체적 액션 1]
2. [구체적 액션 2]
...

[산출물]
- 형식: (HTML/PDF/CSV 등)
- 저장 위치: (Notion/로컬/Drive 등)
- 네이밍 규칙

[검증 포인트]
- Human-in-the-Loop 시점 명시
- 샘플 확인 후 진행

[선택사항]
- 자동화 스케줄
- 알림 설정
```

### Extracted Code (text)

```text
┌─────────────────────────────────────┐
│ 시스템 프롬프트 (항상 동일)         │ ← 캐싱 대상
│ "당신은 HR 전문가입니다..."         │
│ "리더십 평가 규칙은..."             │
│ (2000 토큰)                         │
├─────────────────────────────────────┤
│ 사용자 질문 (매번 변경)             │ ← 새로 처리
│ "김철수의 평가 결과는?"             │
│ (50 토큰)                           │
└─────────────────────────────────────┘

캐싱 전: 2050 토큰 × 100회 = 205,000 토큰
캐싱 후: 2000 토큰(1회 저장) + 50 토큰 × 100회 = 7,000 토큰
절감율: 96.6%
```

### Extracted Code (text)

```text
시스템 프롬프트 (변하지 않음):
- 평가 프레임워크 설명 (1000 토큰)
- 역량 정의 및 평가 기준 (800 토큰)
- 피드백 작성 가이드라인 (500 토큰)
- 회사 가치 및 리더십 철학 (300 토큰)
총: 2600 토큰 → 1회만 전송

동적 부분 (매번 변경):
- 개인 평가 데이터 (100 토큰)
- 구체적 질문 (50 토큰)
```

### Extracted Code (python)

```python
# 캐싱 활성화 예시
response = anthropic.messages.create(
    model="claude-sonnet-4-20250514",
    max_tokens=1024,
    system=[
        {
            "type": "text",
            "text": "평가 프레임워크...",  # 이 부분이 캐싱됨
            "cache_control": {"type": "ephemeral"}
        }
    ],
    messages=[...]
)
```

### Extracted Code (text)

```text
예시:
입력: 전략적사고 18점, 실행력 12점
출력: "전략 수립 역량이 우수합니다(상위 10%). 
실행 단계에서 추진력 강화가 필요합니다. 
권장: 프로젝트 관리 교육 이수"

(구체적 예시 1개로 100 토큰)
```

### Extracted Code (text)

```text
당신은 디지털 전환 교육을 담당하는 전문가입니다.
학습자들의 진도를 모니터링하고 분석해야 합니다.
낙오자가 발생하지 않도록 주의 깊게 살펴야 하며...
(총 800 토큰의 장황한 설명)
```

### Extracted Code (text)

```text
Role: DX 교육 담당자
Task: 진도 모니터링 → 위험군 식별 → 개입
Criteria: 2주 미접속 OR 진도 30% 미만 = 위험군
Output: 독려 메일 초안 (예시 참조)

예시:
[이름]님, DX 모듈2가 멈춰있네요. 
목표일(3/15)까지 [남은일수]일입니다.
도움이 필요하시면 연락 주세요.

(총 150 토큰, 81% 절감)
```

### Extracted Code (python)

```python
def send_email(recipient, subject, body, attachments=None):
    """
    이메일을 발송하는 함수입니다.
    여러 수신자에게 보낼 수 있으며, 
    첨부 파일도 가능합니다.
    """
    ...
```

### Extracted Code (python)

```python
def send_email(
    recipient: str | list[str],
    subject: str,
    body: str,
    attachments: list[str] | None = None
):
    """
    Gmail API를 통해 이메일 발송
    
    Args:
        recipient: 수신자 이메일 (단일 또는 리스트)
            - 단일: "user@example.com"
            - 복수: ["user1@example.com", "user2@example.com"]
        subject: 제목 (최대 200자)
        body: 본문 (HTML 또는 Plain text)
        attachments: 첨부 파일 경로 리스트 (선택)
            - 예: ["/path/to/file.pdf", "/path/to/image.png"]
            - 최대 25MB, 파일당 10MB 제한
    
    Returns:
        dict: {"message_id": "...", "status": "sent"}
    
    Raises:
        ValueError: recipient가 빈 문자열인 경우
        FileSizeError: 첨부 파일이 10MB 초과인 경우
    
    Example:
        >>> send_email(
        ...     recipient="team@company.com",
        ...     subject="Weekly Report",
        ...     body="<h1>Report</h1><p>...</p>",
        ...     attachments=["/reports/week1.pdf"]
        ... )
        {"message_id": "abc123", "status": "sent"}
    """
    ...
```

### Extracted Code (text)

```text
에이전트: create_notion_page(
    title="평가보고서",
    content="김철수님의 평가 결과...",  # 마크다운? HTML?
    database_id="5f2ff32ac1b8..."  # 대시 포함? 제외?
)
→ ERROR: content must be Notion blocks format
```

### Extracted Code (python)

```python
def create_notion_page(
    title: str,
    content: str,
    database_id: str,
    properties: dict | None = None
):
    """
    Notion 데이터베이스에 새 페이지 생성
    
    Args:
        title: 페이지 제목 (최대 2000자)
        content: 페이지 본문 (Notion Markdown 형식)
            - 지원: # 제목, **굵게**, - 리스트
            - 미지원: HTML 태그
        database_id: DB ID (대시 포함/제외 모두 허용)
            - 형식: "5f2ff32a-c1b8-4981-988b-f5e0da6eeb15"
            - 또는: "5f2ff32ac1b84981988bf5e0da6eeb15"
        properties: DB 속성값 (선택)
            - 예: {"주제": "개발", "중요도": "⭐⭐⭐"}
    
    Returns:
        dict: {"page_id": "...", "url": "https://notion.so/..."}
    
    Example:
        >>> create_notion_page(
        ...     title="평가보고서_김철수",
        ...     content="# 평가 결과\n**강점**: ...",
        ...     database_id="5f2ff32ac1b84981988bf5e0da6eeb15",
        ...     properties={"상태": "✅ 완료"}
        ... )
    """
```

### Extracted Code (text)

```text
사용자: 리더십 평가 기준이 뭐였죠?
AI: 전략적 사고, 실행력, 소통능력, 변화관리, 코칭입니다.
사용자: 각각 몇 점이죠?
AI: 각 20점씩, 총 100점입니다.
사용자: 김철수 점수는?
AI: 18, 15, 17, 14, 16점입니다.
사용자: 평균은?
AI: 80점입니다.
...
(50턴의 대화)
```

### Extracted Code (text)

```text
대화 요약:
- 평가 영역: 5개 (각 20점)
- 김철수: 총점 80/100
  - 강점: 전략적사고(18), 소통(17)
  - 개선: 변화관리(14)
- 다음 액션: 피드백 레터 작성 예정
```

### Extracted Code (text)

```text
[높음 - 전문 보존]
최종 결정: 김철수 평가 80점, 승진 추천

[중간 - 요약]
논의: 변화관리 점수 14점 → 코칭 필요성 합의

[낮음 - 삭제]
(에러: CSV 파일 경로 오류 → 재시도 → 성공)
```

### Extracted Code (text)

```text
Chunk 1: 요약 및 종합 점수 (500 토큰)
Chunk 2: 영역별 상세 평가 (3,000 토큰)
Chunk 3: 개선 계획 (1,000 토큰)

필요시 관련 청크만 로드
→ 효율적 메모리 사용
```

### Extracted Code (text)

```text
문장1: "전략적 사고 능력이 뛰어납니다"
문장2: "전략 수립 역량이 우수합니다"
문장3: "장기 비전 설정에 탁월합니다"

→ 유사도 90% 이상
→ 통합: "전략적 사고/비전 수립 역량 우수"
```

### Extracted Code (text)

```text
Level 1: 개인별 요약 (100명 × 50 토큰 = 5,000 토큰)
Level 2: 부서별 평균 (10부서 × 100 토큰 = 1,000 토큰)
Level 3: 전사 종합 (500 토큰)

최종 컨텍스트: 6,500 토큰 (87% 압축)
```

### Extracted Code (text)

```text
┌─────────────────────────────────────────────┐
│ 1. 프롬프트 캐싱                             │
│ - 평가 프레임워크 (1회 로드, 재사용)        │
│ - 피드백 가이드라인 (1회 로드)             │
│ 절감: 2,500 토큰 × 99회 = 247,500 토큰     │
├─────────────────────────────────────────────┤
│ 2. 시스템 프롬프트 최적화                   │
│ - 장황한 설명 800 토큰 → 구조화 150 토큰   │
│ 절감: 650 토큰 × 100회 = 65,000 토큰       │
├─────────────────────────────────────────────┤
│ 3. 툴킷 Docstring 최적화                    │
│ - Notion API 에러 재시도 감소               │
│ - 평균 재시도 2회 → 0.2회                  │
│ 절감: 에러 처리 토큰 90% 감소               │
├─────────────────────────────────────────────┤
│ 4. 컨텍스트 압축                             │
│ - 100명 데이터 50,000 토큰 → 6,500 토큰    │
│ 절감: 43,500 토큰                          │
└─────────────────────────────────────────────┘

총 절감: ~350,000 토큰
비용 절감: ~$3.50 (GPT-4 기준, 1회 실행)
100회 실행 시: $350 절감
```

### Extracted Code (text)

```text
도구: Notion 페이지 생성
잘못된 사용: create_page(title="보고서", content="...")
올바른 사용: create_page(
    title="평가보고서_김철수_2025",
    content="# 평가 결과\n## 강점\n- 전략적사고...",
    database_id="5f2ff32ac1b8..."
)
```

### Extracted Code (text)

```text
프롬프트: "지금까지 논의를 3줄로 요약해줘. 
다음 대화에서 참고할 수 있도록."

AI 응답:
1. 김철수 평가 완료 (80점, 승진 추천)
2. 변화관리 코칭 계획 수립
3. 다음: 박영희 평가 예정
```
