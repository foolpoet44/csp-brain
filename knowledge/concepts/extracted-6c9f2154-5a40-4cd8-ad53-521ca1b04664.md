# Extracted Knowledge from Conv: 6c9f2154-5a40-4cd8-ad53-521ca1b04664

**Date**: 2026-01-02T07:18:34.786666Z

### Extracted Code (text)

```text
1. 평가 데이터 수집 (Excel/API)
   ↓ Notion MCP: notion-create-pages
2. 📊 리더십 평가 데이터 DB에 저장
   ↓ Python 분석 스크립트 실행
3. 분석 리포트 생성 (Claude + XLSX Skill)
   ↓ Notion MCP: notion-create-pages
4. 💬 Claude 대화 아카이브에 저장
   ↓ 
5. 평가 데이터와 리포트 양방향 링크
```

### Extracted Code (text)

```text
💬 Claude 대화 아카이브 ←→ 📊 리더십 평가 데이터
        ↓                              ↓
    (분석 리포트)              (피드백 세션 기록)
        ↓                              ↓
    🎓 DX 교육 프로그램 ←→ ⚙️ HR 자동화 워크플로우
```

### Extracted Code (python)

```python
# 1. 평가 데이터 수집 (매주 월요일 자동 실행)
# → Notion MCP: notion-create-pages로 데이터 저장

def auto_collect_leadership_data():
    # SAP SF에서 데이터 가져오기
    eval_data = fetch_sap_sf_evaluations()
    
    # Notion에 일괄 저장
    for record in eval_data:
        notion.pages.create(
            parent={"data_source_id": "dfc8838d-d226-4987-aac8-dc06250267f3"},
            properties={
                "Name": f"{record['name']} - {record['year']} {record['round']}",
                "평가자 ID": record['evaluator_id'],
                "평가 연도": record['year'],
                "평가 라운드": record['round'],
                "자기성찰 점수": record['self_reflection'],
                "커뮤니케이션 점수": record['communication'],
                "타인존중 점수": record['respect'],
                "종합 점수": record['total_score']
            }
        )

# 2. 분석 실행 및 리포트 생성
# → Claude + XLSX Skill

# 3. 결과를 Claude 대화 아카이브에 저장
# → Notion MCP: notion-create-pages

# 4. 피드백 세션 일정 제안
# → Gmail 자동 발송
```

### Extracted Code (python)

```python
# Notion MCP: notion-search로 데이터 조회
# 특정 교육생의 리더십 평가 + DX 교육 이력 연결

def analyze_education_impact(student_id):
    # 1. 교육 이력 조회
    education_data = notion.search(
        data_source_url="collection://c479a532-f8d2-4743-8418-a6ef1a6e92b8",
        query=f"교육생 ID: {student_id}"
    )
    
    # 2. 해당 기간 리더십 평가 조회
    eval_before = get_evaluation(student_id, before_education)
    eval_after = get_evaluation(student_id, after_education)
    
    # 3. 개선도 계산 및 시각화
    improvement = calculate_improvement(eval_before, eval_after)
    
    # 4. 리포트 생성 및 저장
    create_impact_report(student_id, improvement)
```

### Extracted Code (markdown)

```markdown
# 📊 리더십 평가 현황 대시보드

## View 1: 평가 진행 현황
- Filter: 피드백 완료 = false
- Group By: 직급
- Sort: 평가 연도 DESC

## View 2: 개선 영역 분석
- Filter: 개선 영역 is not empty
- Group By: 개선 영역
- Chart: Bar Chart

## View 3: 부서별 평균 점수
- Group By: 소속 조직
- Calculate: Average of 종합 점수
- Chart: Line Chart (시계열)
```

### Extracted Code (sql)

```sql
-- Rollup 예시: DX 교육 프로그램에서
-- 관련된 리더십 평가의 평균 개선도 계산

Rollup 속성 설정:
- Relation: 관련 리더십 데이터
- Property: 종합 점수
- Calculate: Average
```

### Extracted Code (python)

```python
# ❌ 나쁜 예: 100번 API 호출
for person in people:
    notion.pages.create(...)

# ✅ 좋은 예: 1번에 최대 100개 생성
notion.pages.create_batch(
    pages=[...],  # 최대 100개
    parent={"data_source_id": "..."}
)
```

### Extracted Code (python)

```python
# 1. 데이터 조회
existing_data = notion.fetch(database_id)

# 2. 로컬에서 처리
processed_data = analyze(existing_data)

# 3. 필요한 부분만 업데이트
for page in pages_to_update:
    notion.update_page(page_id, new_properties)
```

### Extracted Code (python)

```python
import logging
from datetime import datetime

def safe_notion_create(data):
    try:
        result = notion.pages.create(...)
        log_success(result)
        return result
    except Exception as e:
        # ⚙️ HR 자동화 워크플로우의 "오류 로그"에 기록
        notion.pages.update(
            workflow_page_id,
            properties={
                "오류 로그": f"[{datetime.now()}] {str(e)}",
                "유지보수 필요": "__YES__"
            }
        )
        alert_admin(e)
```

### Extracted Code (markdown)

```markdown
# 리더십 평가 트렌드 대시보드

## 차트 1: 분기별 평균 점수
- Type: Line Chart
- X-axis: 평가 라운드
- Y-axis: Average(종합 점수)

## 차트 2: 직급별 역량 분포
- Type: Bar Chart
- Group: 직급
- Values: 자기성찰, 커뮤니케이션, 타인존중

## 차트 3: 개선 영역 우선순위
- Type: Table
- Columns: 개선 영역, Count
- Sort: Count DESC
```

### Extracted Code (python)

```python
# Notion → Excel → Power BI 파이프라인
def export_to_powerbi():
    # 1. Notion 데이터 추출
    data = notion.search(data_source_url="...")
    
    # 2. Excel 변환 (XLSX Skill)
    excel_file = create_excel_report(data)
    
    # 3. Power BI로 자동 업로드
    upload_to_powerbi(excel_file)
```

### Extracted Code (python)

```python
# 매주 금요일 17:00 자동 실행
def weekly_data_quality_check():
    # 1. 데이터 품질 검증
    issues = check_data_quality()
    
    # 2. Claude에게 분석 요청
    insights = claude.analyze(issues)
    
    # 3. 개선 제안을 Claude 대화 아카이브에 저장
    save_to_archive(insights)
    
    # 4. HR 팀에게 리포트 발송
    send_weekly_report()
```
