# Extracted Knowledge from Conv: e6f6a33b-8132-4579-91fd-9fcd9213aa14

**Date**: 2025-09-23T05:00:38.147477Z

### Extracted Code (bash)

```bash
# RHEL/CentOS 8+
sudo dnf install ibus ibus-hangul
sudo dnf install @korean-support

# RHEL/CentOS 7
sudo yum install ibus ibus-hangul
sudo yum groupinstall korean-support

# Fedora
sudo dnf install ibus ibus-hangul
sudo dnf install langpacks-ko
```

### Extracted Code (bash)

```bash
# 시스템 로케일 확인
localectl status

# 한국어 로케일 설정
sudo localectl set-locale LANG=ko_KR.UTF-8

# 또는 영어 시스템에서 한글만 입력
export LANG=en_US.UTF-8
export LC_CTYPE=ko_KR.UTF-8
```
