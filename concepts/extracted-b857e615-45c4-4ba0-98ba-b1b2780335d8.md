# Extracted Knowledge from Conv: b857e615-45c4-4ba0-98ba-b1b2780335d8

**Date**: 2026-04-10T22:02:31.306719Z

### Extracted Code (text)

```text
CSP의 빌드 워크로드
        │
        ▼
 ┌─────────────┐
 │  작업 분류  │
 └──────┬──────┘
        │
   ┌────┴────┐
   ▼         ▼
판단 필요   반복 작업
   │         │
Claude     Gemini
 Code      (무료)
```

### Extracted Code (bash)

```bash
# 이걸 ~/.zshrc에 추가
export GEMINI_API_KEY="여기에_키"

alias bifrost='GEMINI_API_KEY=$GEMINI_API_KEY npx -y @maximhq/bifrost &'
alias cc='ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude'
alias cc-gemini='ANTHROPIC_BASE_URL=http://localhost:8080/anthropic claude --model gemini/gemini-2.0-flash'
```

### Extracted Code (bash)

```bash
bifrost        # 프록시 백그라운드 실행
cc             # Claude Code (Claude 모드)
cc-gemini      # Claude Code (Gemini 모드)
```

### Extracted Code (text)

```text
아침에 개발 시작
    │
    ▼
bifrost 실행 (한 번만)
    │
    ▼
cc-gemini으로 작업 시작
(보일러플레이트, 반복 코드)
    │
    ▼
복잡한 문제 발생?
    │
    ▼
cc로 전환
(Claude에게 핵심 판단만 위임)
    │
    ▼
해결되면 다시 cc-gemini로
```
