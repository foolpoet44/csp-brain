# Extracted Knowledge from Conv: 86256888-9a32-4a28-bb2e-6435f1770bd1

**Date**: 2026-04-01T05:42:45.749819Z

### Extracted Code (text)

```text
[기존]
CSP 맥북 → Claude Code → Local MCP Server → 파일/도구

[Remote MCP]
어디서든 → Claude → Remote MCP Server (클라우드) → 파일/도구/API
```

### Extracted Code (text)

```text
CSP 모바일/다른 기기
    ↓ 명령 전송
원격 서버 (EC2, Fly.io, Railway 등)
    ↓ Claude Code 실행
    ↓ 코드 작성, 테스트, 배포
결과 → CSP에게 반환
```

### Extracted Code (bash)

```bash
# 맥북에서 최초 세션 생성
tmux new -s claude-agent

# 이 안에서 Claude Code를 --print 모드로 실행
# --print는 대화형이 아닌 단발성 실행 모드
claude --print "ESCON의 Temporal Knowledge Graph 스키마 검토하고 문제점 리포트해줘"
```

### Extracted Code (yaml)

```yaml
# .github/workflows/claude.yml
name: Claude Code Agent

on:
  issue_comment:
    types: [created]

jobs:
  claude:
    if: contains(github.event.comment.body, '@claude')
    runs-on: ubuntu-latest
    steps:
      - uses: anthropics/claude-code-action@beta
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
```
