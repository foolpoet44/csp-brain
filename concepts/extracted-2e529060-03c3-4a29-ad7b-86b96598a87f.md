# Extracted Knowledge from Conv: 2e529060-03c3-4a29-ad7b-86b96598a87f

**Date**: 2025-10-27T02:11:55.206800Z

### Extracted Code (bash)

```bash
# 현재 활성화된 Python 버전 확인
pyenv version

# 사용 가능한 Python 버전 목록
pyenv versions

# 현재 Python 경로 확인
which python
```

### Extracted Code (bash)

```bash
# libffi.so.7이나 libffi.so.8이 있다면
sudo ln -s /usr/lib64/libffi.so.8 /usr/lib64/libffi.so.6
# 또는
sudo ln -s /usr/lib/x86_64-linux-gnu/libffi.so.8 /usr/lib/x86_64-linux-gnu/libffi.so.6
```

### Extracted Code (bash)

```bash
# pyenv 대신 conda 사용
conda create -n org_report python=3.10
conda activate org_report
pip install -r requirements.txt
python main.py
```

### Extracted Code (bash)

```bash
# 현재 시스템의 libffi 버전 확인
ls -la /usr/lib64/libffi.so*

# 있는 버전으로 환경변수 설정
export LD_LIBRARY_PATH=/usr/lib64:$LD_LIBRARY_PATH
python main.py
```

### Extracted Code (bash)

```bash
# libffi 찾기
find /usr -name "libffi.so*" 2>/dev/null

# 찾은 경로에 libffi.so.7 또는 .8이 있다면
export LD_LIBRARY_PATH=/usr/lib64:$LD_LIBRARY_PATH
# 또는
export LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH

python main.py
```

### Extracted Code (bash)

```bash
# Miniconda 설치 (sudo 권한 불필요)
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh -b -p $HOME/miniconda3

# conda 초기화
$HOME/miniconda3/bin/conda init bash
source ~/.bashrc

# 새 환경 생성
conda create -n org_report python=3.10
conda activate org_report
pip install -r requirements.txt
python main.py
```

### Extracted Code (bash)

```bash
# 현재 환경 삭제
pyenv uninstall myenv

# Python 재설치 (빌드 시 libffi 포함하도록)
CONFIGURE_OPTS="--enable-shared" pyenv install 3.10.10

# 새 가상환경 생성
pyenv virtualenv 3.10.10 myenv
pyenv activate myenv
pip install -r requirements.txt
```

### Extracted Code (bash)

```bash
# 예: /usr/lib64/libffi.so.8 을 찾았다면
export LD_LIBRARY_PATH=/usr/lib64:$LD_LIBRARY_PATH

# 테스트
python main.py
```

### Extracted Code (bash)

```bash
pyenv uninstall myenv
PYTHON_CONFIGURE_OPTS="--enable-shared" pyenv install 3.10.10
pyenv virtualenv 3.10.10 myenv
pyenv activate myenv
pip install -r requirements.txt
python main.py
```

### Extracted Code (bash)

```bash
# data_processor.py 파일에서 클래스 이름 확인
grep -n "class.*:" /nas/users/PR/dk.cho/Downloads/Data/leadership/리포트_251020/data_processor.py

# 또는 파일 내용 일부 보기
head -50 /nas/users/PR/dk.cho/Downloads/Data/leadership/리포트_251020/data_processor.py
```

### Extracted Code (bash)

```bash
# 현재 디렉토리 확인
pwd

# main.py와 data_processor.py 위치 확인
ls -la main.py
ls -la data_processor.py
ls -la /nas/users/PR/dk.cho/Downloads/Data/leadership/리포트_251020/data_processor.py

# data_processor.py의 클래스/함수 확인
grep "^class\|^def" /nas/users/PR/dk.cho/Downloads/Data/leadership/리포트_251020/data_processor.py
```
